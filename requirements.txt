# FDLC — pattern engine
ezdxf>=1.3.0          # DXF generation (Mode A + B)
svgelements>=0.9.0    # SVG path parsing (Mode A)
shapely>=2.0.0        # Geometry operations, seam allowance (Mode A)
pyclipr>=0.1.0        # Polygon clipping for seam allowance (Mode A)

# Image generation
Pillow>=10.0.0        # Image handling

# Archive sweep
requests>=2.31.0      # HTTP calls (Gemini API, Ollama)

# Research extraction
# (uses Ollama local — no pip package needed)

# Note: Anthropic + Google APIs accessed via OpenClaw providers
# No raw API keys needed in scripts — use Bitwarden skill for BFL key
